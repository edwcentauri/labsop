# AGENTS.md

## 项目概览

这是一个面向移动端的实验室 SOP 网站，使用 Vite、React 19 和 TypeScript 构建，包含 SOP 内容、在线计算工具和站内 PDF 阅读器。

## 常用命令

- 安装依赖：`npm install`
- 启动开发服务器：`npm run dev`
- 运行测试：`npm test`
- 运行静态检查：`npm run lint`
- 类型检查并构建：`npm run build`
- 生成示例 PDF：`npm run generate:pdfs`

以上命令仅供人类维护者使用。代理提交改动前不得运行任何本地测试、静态检查、类型检查、构建、预览或手动 UI/PDF 验证；测试与验收由人类负责。

## 目录与职责

- `src/App.tsx`：主要页面结构与路由入口。
- `src/data.ts`：SOP 与计算工具的结构化数据。
- `src/calculations.ts`：计算公式与纯函数逻辑。
- `src/calculations.test.ts`：计算逻辑测试。
- `src/PdfViewer.tsx`：站内 PDF 阅读器。
- `src/content/`：公告及维护类 Markdown 内容。
- `src/styles.css`：全局样式与响应式布局。
- `public/pdfs/`：网站直接提供的 PDF 文件。
- `scripts/generate-demo-pdfs.mjs`：示例 PDF 生成脚本。
- `dist/`：构建产物，不要手工编辑。

## 开发约定

- 保持 TypeScript 严格类型；不要用 `any` 绕过可表达的类型问题。
- 优先编写小型、可测试的纯函数；计算逻辑放在 `src/calculations.ts`，不要散落在 UI 组件中。
- 修改公式、单位换算、边界值或舍入规则时，同步补充或更新 Vitest 测试。
- 沿用现有 React 函数组件和数据驱动结构，避免为简单功能引入新的状态管理或 UI 框架。
- 保持移动端优先，并检查窄屏下的导航、表单、表格和 PDF 阅读体验。
- Vite 的部署基础路径是 `/labsop/`；新增资源时使用兼容该基础路径的引用方式，不要假设网站部署在域名根目录。
- 不要编辑 `node_modules/` 或 `dist/`，也不要提交临时文件、调试输出或本地配置。

## SOP 内容与安全性

- 实验步骤、参数、警告、版本号和生效日期属于受控内容；除非用户提供了已批准来源，否则不要自行补全或臆造。
- 更新 SOP 数据、公告或 PDF 时，保持标题、版本号、生效日期、文件名和站内链接一致。
- `public/pdfs/` 中的示例文件只用于演示。替换正式文件时，不要覆盖或改写用户未明确指定的受控文件。
- 对可能影响实验结果或人员安全的修改，在交付说明中明确指出，并说明采用的来源或假设。

## 测试与验证约定

- 所有提交在提交前都完全不进行本地测试或验证；改动完成后直接提交并推送功能分支，再向用户提供手动创建 PR 的链接。
- 代理不得运行 `npm test`、`npm run lint`、`npm run build`、开发服务器、预览服务器、PDF 生成脚本或其他验证命令。
- 代理可以新增或更新测试文件，但不得执行测试。
- 测试、构建、页面检查、PDF 阅读器检查和最终验收均由人类维护者负责。
- 交付说明必须明确写明“未运行本地测试（按项目约定）”，不得暗示改动已经通过测试或验证。

## PR 链接与分支清理

- 每次开始新的编辑任务时，不枚举、比较或核查既有本地功能分支，也不以任何本地分支作为修改基线。
- 开始编辑前直接刷新远端 `main`，并从最新 `origin/main` 创建本次任务的独立功能分支；不得在本地 `main` 上直接修改或提交。
- 上述规则不免除对未提交更改的保护；若当前工作区存在用户改动，必须保留并避免覆盖。
- 不得直接向 `main` 提交或推送改动；每项改动都必须在独立的功能分支上完成。
- 改动完成后由代理提交并推送功能分支，但代理不得通过 GitHub API、连接器、`gh` 命令或浏览器代替用户创建、合并或设置 PR。
- 推送成功后，代理必须生成并交付以 `main` 为目标分支的 GitHub compare 链接，格式为 `https://github.com/<owner>/<repo>/compare/main...<branch>?expand=1`，由用户手动创建 PR。
- 交付 compare 链接时，同时提供建议的 PR 标题、改动摘要，并明确注明“未运行本地测试（按项目约定）”。
- PR 的 squash 合并、检查审批及合并后删除远程源分支均由用户在 GitHub 中手动完成；代理不得启用自动合并或绕过保护规则。
- 用户确认 PR 已合并前，代理必须保留对应的本地与远程功能分支。
- 用户确认 PR 已合并后，代理应同步 `main`，并确认该任务提交已经进入 `origin/main`，再清理由代理为该任务创建且已经合并的本地与远程功能分支。
- 不得删除 `main`、长期维护分支、尚未合并的分支、其他人创建的分支，或无法确认归属和合并状态的分支。
- 若 compare 链接无法生成或分支推送失败，保留本地提交与分支并在交付说明中报告状态，不得改用其他方式代建 PR。

## 修改原则

- 只修改完成当前任务所需的文件，保留用户已有的未提交更改。
- 优先做范围小、可审查的改动；不要顺手重构无关代码。
- 新增依赖前先确认现有依赖或浏览器能力无法满足需求，并说明新增依赖的理由。
- 若需求与受控 SOP 内容、现有测试或部署约束冲突，先明确冲突再继续。
